spring:
  cloud:
    nacos:
      config:
        username: nacos
        password: nacos
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud
      discovery:
        username: nacos
        password: nacos
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud
