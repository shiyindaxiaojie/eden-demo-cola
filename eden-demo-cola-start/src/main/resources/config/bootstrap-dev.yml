spring:
  cloud:
    nacos:
      config:
        enabled: false
        username: nacos
        password: nacos
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud
      discovery:
        enabled: false
        username: nacos
        password: nacos
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud
