spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud
        username: nacos
        password: nacos
      discovery:
        server-addr: localhost:8848
        namespace: demo
        group: spring-cloud
        username: nacos
        password: nacos
