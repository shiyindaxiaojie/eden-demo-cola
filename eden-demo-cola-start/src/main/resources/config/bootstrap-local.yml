spring:
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        namespace: demo
        group: ${spring.application.name}
        username: nacos
        password: nacos
      discovery:
        server-addr: localhost:8848
        namespace: demo
        group: ${spring.application.name}
        username: nacos
        password: nacos
