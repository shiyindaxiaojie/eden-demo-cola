spring:
  cloud:
    nacos:
      discovery: # 注册中心
        enabled: false # 默认关闭，请按需开启
        server-addr: localhost:8848
        namespace: demo
        group: demo
        username: nacos
        password: nacos
      config: # 配置中心
        enabled: false # 默认关闭，请按需开启
        server-addr: localhost:8848
        namespace: demo
        group: demo
        username: nacos
        password: nacos
